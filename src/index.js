'use strict';

let {
    object, array, a, o, objectMap, arrayList, om, al, isInternelObject
} = require('./ds');

let jsonObjectToCData = require('./jsonObjectToCData');

module.exports = {
    object, array, a, o, objectMap, arrayList, om, al,
    jsonObjectToCData, isInternelObject
};
